root = "."
tmp_dir = "/tmp/air"

[build]
  cmd = "go build -o /tmp/air/main ./cmd/api/main.go"
  bin = "/tmp/air/main"
  exclude_dir = ["vendor"]
  include_ext = ["go", "tpl", "tmpl", "html"]
  delay = 1000

